import { SxProps } from "@mui/material"

const containerSX: SxProps = {
  pointerEvents: "none",
  outline: "none !important",
  border: "none !important",
}

const modalSX: SxProps = {
  zIndex: "auto",
}

export { modalSX, containerSX }
